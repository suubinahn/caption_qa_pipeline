# 알려진 한계 (Known Limitations)

MSVD/Wikimedia 20개 표본(정답 20개 + 오답 60개)으로 검증하며 실제로 재현·확인한
`inspect_caption_full()` 파이프라인의 구조적 한계를 정리한다. 전부 최소 1개 이상의
구체적 실패 사례로 확인된 것만 적었다 — 추측이나 이론적 가능성은 넣지 않았다.

회사 주행영상 파일럿을 시작하기 전에 반드시 읽을 것. 특히 3번(캡션 언어 의존성)은
파일럿 캡션에 도로명/표지판 등이 섞여 들어간다면 바로 영향을 받는다.

---

## 1. 순간적 사건 동사(event verb)는 프레임을 아무리 늘려도 못 잡을 수 있다

**증거**: `train_arriving` 샘플("A commuter train arrives at a station platform.")의
정답 캡션이 **문장 단위 검증 자체에서 FAIL**했다 (5개 프레임 점수: 0.03 / 0.17 /
0.43 / 0.44 / 0.36 — 전부 PASS 기준선 0.9560에 한참 못 미침).

**원인**: "달린다/논다"처럼 지속되는 상태 동사는 프레임 하나에 시각적 증거(다리 위치,
움직임 방향 등)가 남지만, "도착하다/출발하다"처럼 **순간적 사건(event) 동사**는 정지
이미지 어디에도 "이게 도착 중인지, 정차 중인지, 출발 직전인지" 구별할 시각적 증거가
없을 수 있다. 5~6단계에서 만든 다중 프레임 max-pooling(각 5장 중 최댓값 채택)도 이
문제는 못 고친다 — "5장 중 확실한 증거를 담은 1장"이 애초에 존재하지 않기 때문이다.

**주행영상 적용 시 위험도: 높음.** "정지선에서 멈춘다", "차선을 변경하기 시작한다",
"신호가 바뀌는 순간" 같은 사건성 서술이 흔할 텐데, 정확히 이 유형에 해당한다.

---

## 2. 상태 전환이 있는 영상에서 행동 오류를 놓친다 (PASS 분기에서 특히 취약)

**증거**: 최초 발견은 `cat_string_toy`(논다→잔다) 1건이었지만, 표본을 20개로
넓히자 신규 12개 중 **5건**에서 동일 패턴이 나왔다:
- `soccer_kick`: "가만히 서있다"(0.9978) 오답을 PASS로 오판
- `band_stage`: "장비 챙겨서 떠난다"(0.9970) **및** "빨간 조명"(0.9935) **둘 다** 오판
- `cat_wakeup`: "눈을 감으며 잠든다"(0.9612) 오답을 PASS로 오판 — cat_string_toy와
  동일 유형(상태 전환)이 재현됨
- `children_playground`: "가만히 조용히 서있다"(0.9761) 오답을 PASS로 오판
- `jellyfish_aquarium`: "빠르게 왔다갔다 한다"(0.9980) 오답을 PASS로 오판

기존 8개에서는 1/8(12.5%), 신규 12개에서는 5/12(41.7%) — **표본이 늘어나며 오히려
악화됐다.** 8개짜리 초기 검증이 이 약점을 과소평가했다는 뜻이다.

**원인**: `full_inspector.py`의 PASS 분기에 있는 "문장 점수가 이미 높으면(≥0.95)
행동 검증을 더 관대하게 본다"는 안전장치가, PASS 판정 자체의 하한선(0.9560)이 이미
0.95보다 높아서 **PASS 분기에 도달하는 모든 문장에 사실상 항상 적용된다.** 즉 설계
의도(애매한 경우만 관대하게)와 다르게 사실상 무조건 관대해지는 셈이라, 행동 오류를
잡을 기회가 원천적으로 줄어든다. (코드 주석 `SENTENCE_HIGH_CONFIDENCE_CUTOFF` 참고 —
다음 재보정 때 이 메커니즘 자체를 재설계해야 한다.)

**주행영상 적용 시 위험도: 높음.** "정차했다가 출발한다", "서행하다가 급가속한다"
같은 상태 전환 서술은 주행영상에서 매우 전형적이다.

---

## 3. 여러 인물/사물이 동시에 다른 행동을 할 때 주어 결합(subject binding) 오류

**증거**: `guitar_play`(케리 장관이 기타를 치는 장면, 뒤에 합창단처럼 보이는 여성들이
서있음)에서, "playing"(정답)이 5개 프레임 **전부**에서 "singing"(오답, 배경 인물이
노래하는 것처럼 보임)에게 졌다 (다수결 5:0). `band_stage`도 같은 위험군으로 넣었는데
오답 2개를 전부 놓쳤다.

**원인**: 객체/행동 단위 검증은 "이 단어가 이미지 어딘가에서 사실인가"만 물을 뿐,
"그게 캡션이 말하는 그 주어의 행동/속성인가"는 확인하지 않는다. 화면에 여러 인물이
있고 그중 한 명만 캡션의 주어라면, 다른 인물의 행동이 캡션의 행동인 것처럼 오인될 수
있다. 이건 NLTK 단순 품사 분석의 한계다 — 진짜 문법적 주어를 찾으려면 의존구문분석
(dependency parsing)이 필요한데 이번 파이프라인 범위 밖이다.

**주행영상 적용 시 위험도: 중간.** 차량 1대만 있는 단순 장면보다는, "여러 차량이
동시에 다른 행동을 하는" 교차로/합류 구간 영상에서 위험이 커진다 (예: "앞차가
정지했다"고 썼는데 다른 차선의 차가 움직이는 걸 보고 혼동할 수 있음).

---

## 4. 캡션 언어 의존성 — 문장 일부에 한글 토큰이 섞여도 위험하다

**확인된 사실**: 객체/행동 추출(`object_check.py`, `action_check.py`)은 NLTK 영어
품사 태거를 쓴다. 문장 전체가 한국어면 크래시 없이 모든 단어가 고유명사(NNP)로
오분류되고 동사는 0개 추출된다 (초기 테스트로 확인).

더 중요한 건: **문장은 영어인데 도로명/표지판처럼 고유명사 일부만 한글인 경우**도
위험하다는 것을 실제로 확인했다. 같은 단어("강남대로")를 문장 내 위치만 바꿔 3번
테스트했더니:

| 문장 | "강남대로"의 태그 | 결과 |
|---|---|---|
| "...at the 강남대로 intersection..." | JJ (형용사) | 안전 (필터링됨) |
| "The truck passes the 강남대로 sign..." | **NNP (고유명사)** | **명사로 추출됨 - 위험** |
| "A road sign says 강남대로 near..." | CD (숫자) | 안전 (필터링됨) |

3번 중 1번은 실제로 명사로 오추출돼 `object_check`이 "a photo of a 강남대로" 같은
의미 없는 질의를 만든다. 이 질의는 당연히 낮은 점수를 받아 **멀쩡한 캡션을 SUSPECT로
잘못 찍어 REVIEW로 강등시킬 수 있다.** 어느 위치에서 안전하고 어느 위치에서 위험한지
예측할 수 없다는 게 핵심 문제다 (한글에 대해 전혀 학습되지 않은 태거의 통계적 추측이라
문맥에 따라 완전히 달라짐).

**주행영상 적용 시 위험도: 확인 필요.** 현재 회사 주행영상 캡션은 영어로 확인됐지만,
도로명/표지판/상호명 등 고유명사가 원문(한글) 그대로 섞여 들어가는 경우가 있다고
확인됨 — 그런 캡션이 섞여 있다면 이 위험이 그대로 적용된다. `full_inspector.py`에
`_warn_if_likely_non_english()` 안전장치를 넣어 한글 토큰이 섞인 캡션을 만나면 콘솔에
경고를 출력하도록 해뒀다 (자동 차단은 아니고 경고만 함 - 실제 영향도는 파일럿 결과를
봐야 안다).

---

## 참고: 아직 검증되지 않은 것 (한계로 확정된 건 아니지만 파일럿 전 점검 필요)

- **영상 길이 가정**: 지금까지 테스트한 영상은 2.5초~90초짜리 짧은 클립이었다.
  주행영상이 훨씬 길다면(예: 수 분 단위), "전체 영상의 10~90% 구간에서 5장 균등
  샘플링"이라는 현재 방식이 적절한지 검증되지 않았다 — 특정 이벤트(급정거 등)가
  전체 영상의 아주 짧은 구간에서만 일어난다면 5장으로는 놓칠 수 있다.
- **도메인 전이**: 스타일 판별기(`style_detect.py`)는 "사진/카툰/일러스트"만 구분한다.
  주행영상 특유의 조건(야간, 우천, 역광, 블랙박스 화질)에서 BLIP-ITM 점수 분포가
  지금까지의 임계값(0.9101/0.9560, 0.0214)과 얼마나 다를지는 검증되지 않았다 —
  파일럿 초기에는 자동 판정을 그대로 믿지 말고, 사람 검수와 병행 비교를 권장한다.

---

## 원본 근거 데이터

- 20개 샘플 전체 결과: `outputs/stage16_largescale_results.csv`
- 문장 단위 임계값 보정 과정: `outputs/stage7_all_scores.csv` (calibrate_thresholds.py)
- 객체/행동 단위 임계값 보정 과정: `outputs/component_dataset_scored.csv` (calibrate_component_thresholds.py)
- 종합 리포트(1~6단계): `results/final_report.md`

---
---

# 검색+검증 통합 파이프라인 (search_module.py / search_and_verify.py)

위 1~4번은 `full_inspector.py`(캡션 사실검증 단독 파이프라인)의 한계다. 이후 목표가
"검색어로 찾은 캡션이 실제 영상과 맞는가"까지 검증하는 것으로 확장되면서, ①검색어↔캡션
(search_module.py)과 ②캡션↔영상(search_and_verify.py) 두 축이 새로 생겼다. 이 절은 그
두 축에서 실제로 확인한 한계를 정리한다. 최종 통합 파이프라인(`search_and_verify.py`)을
쓰기 전에 반드시 읽을 것.

## 5. CLIP 텍스트 인코더는 상태/동작 동사 구분에 약하다 (①)

**증거**: "정지"/"좌회전" 검색에서, 완전히 무관한 문장("moving forward")이 진짜 정답
문장보다 더 높은 코사인 유사도를 받는 사례를 발견했다("좌회전" 검색 1위 0.9135 vs 진짜
좌회전 문장 0.9104). 회사 캡션들이 거의 동일한 문장 템플릿을 공유해서, CLIP이 핵심
동사 하나보다 문장 전체 표면 어휘 유사도에 더 좌우되는 것으로 추정된다. CSLS 허브
보정으로 일부 완화되지만(9개 검색어 전부 개선) "정지"는 보정 후에도 최상위 오답이
남았다 — 근본 해결은 아니다.

**주행영상 적용 시 위험도: 높음.** "정지"/"차선 변경"처럼 상태·동작을 특정하는 검색어일수록
관련 없는 결과가 상위에 섞일 가능성이 높다.

## 6. road_type처럼 라벨 경계가 애매한 카테고리는 "실패"로 보이지만 라벨 문제일 수 있다 (①)

**증거**: "다차선고속도로" 검색이 랜덤보다 나쁜 정밀도(19.5% vs 41%)를 보였는데, 실제
프레임을 확인하니 고속도로처럼 보이는 장면이 'three-lane road'로 라벨링돼 있었다.
'multi-lane highway'+'three-lane road'를 합쳐 재검증하면 precision 수치는 크게 오르지만
(19.5%→76.9%), 병합 카테고리의 랜덤 베이스라인도 같이 뛰어서 lift로 보면 오히려 거의
랜덤(0.99배)과 같아진다 — 라벨 모호성이 유일한 원인은 아니고, 모델이 "고속도로스러움"을
근본적으로 잘 못 구분하는 약점도 같이 있다.

## 7. info.txt의 특정 필드는 캡션에 그대로 주입되어 있다 (①)

**증거**: `road_context`(85~97%)와 `surface`(92~95%) 필드 값이 캡션 문장에 거의 그대로
등장한다. 즉 이 필드 관련 검색어는 "영상을 진짜 이해해서 찾는" 것이 아니라 "주입된
문구를 문자열 검색하는" 것에 가깝다. `motion`/`road_type`/`time_of_day`는 상대적으로
덜 주입되어(19~66%) 더 신뢰할 만한 테스트다.

## 8. 한국어 검색어는 번역을 거쳐도 완벽하지 않다 (①)

**증거**: CLIP 자체는 한국어를 이해 못 해서(직접 입력 시 "터널 통과" 81.2%→6.2%로 붕괴),
번역 전처리(Helsinki-NLP/opus-mt-ko-en)를 거치는데, 9개 검색어 중 7개는 영어 원본
수준으로 회복되지만 번역 품질 자체가 완벽하지 않다("정지해 있는 장면"→"Car's still on."
같은 오역 존재). 번역된 문장은 `VALIDATED_QUERY_THRESHOLDS`의 등록된 영어 문자열과
정확히 일치해야 컷오프가 적용되므로, 번역문에는 컷오프가 안 걸리고 순위만 나온다.

## 9. 컷오프(이분류 임계값)는 9개 중 2개 검색어에만 있다 (①)

**증거**: 100클립 표본 calibration/holdout으로는 "터널"만 안정적으로 검증됐다. 회사
전체 740클립으로 재검증해서 "비"가 추가로 검증됐지만(양성 샘플이 35→240으로 늘어난
덕분), 나머지 7개(우회전/좌회전/정지/다리/야간/눈/다차선고속도로)는 희귀 카테고리라
전체 데이터로 확장해도 양성 샘플 수가 그대로라 여전히 불안정하다 — 특히 "야간"은
100클립 기준 "약하게 검증됨"이었다가 740클립에서 precision이 54.5%→5.7%로 무너진
사례가 있다(작은 negative pool에서 나온 착시였음). **검증 안 된 7개 검색어는 컷오프
없이 순위만 신뢰할 것.**

## 10. [부분 해결] CLIP 텍스트 인코더는 부정문(negation)을 구분하지 못한다 (①)

**증거**: 검증된 9개 검색어 밖의 자유형 쿼리로 실전 테스트하다가 발견했다.
"There is heavy traffic congestion."(교통 정체) 검색에서, 실제로는 **정반대
내용인** "The traffic is moderate, and the road is not congested."(정체
아님)라는 문장이 검색 1위로 뜨고 "일치" 판정까지 받았다. 직접 코사인
유사도를 재보니 원문과 그 부정문("The road is not congested.")이
**0.9248**로, 원문을 자기 자신과 비교한 값(1.0000)에 근접했다 - 사실상
구분을 못 하는 수준. "차선을 변경한다" vs "차선 변경 없음"에서도 동일한
패턴(0.9251)을 확인했다.

**원인**: CLIP류 텍스트 인코더가 "not", "no" 같은 부정어보다 문장의 나머지
표면 어휘("congestion", "lane change" 등 핵심 명사/동사)에 더 크게 좌우되는,
널리 알려진 구조적 약점이다 - 이 프로젝트에서 반복 확인된 "상태동사/부정
같은 미묘한 의미 차이에 약하다"는 패턴의 연장선.

**주행영상 적용 시 위험도(완화 전): 높음.** "사고가 없다", "정체가 아니다",
"보행자가 없다"처럼 **부정형으로 서술된 캡션**이 있다면, 그 반대 의미의
검색어에도 버젓이 상위로 뽑힐 수 있었다. 지금까지 검증된 9개 검색어는
전부 긍정문이라 이 문제를 못 잡아냈다.

**완화책 적용됨**: `search_module.py`에 `penalize_negation`(기본값 True) 옵션을
추가했다 - 검색어 핵심 단어(어형 변화 포함, 예: "congestion"↔"congested")
**앞쪽**에 부정어가 있는 문장은 그 클립의 대표 문장 후보에서 사실상 배제한다.
부정어 방향을 "키워드 앞"으로 제한한 이유: 처음엔 앞뒤 다 봤다가 "a clear
night with no stars visible"에서 "no"가 실제론 "stars"를 부정하는데
"night"까지 오탐하는 문제를 실측으로 발견해서 고침. 9개 검증 검색어로
회귀 테스트한 결과 8개는 precision 변화 없음, "터널" 하나만 81.2%->75.0%로
떨어졌는데 - 확인해보니 이건 오탐이 아니라 **실제로 그 클립의 캡션 전체에
터널 얘기가 "터널이 안 보인다"밖에 없었던 경우**였다(info.txt는
tunnel이라고 하는데 캡션이 놓친 사례) - 즉 예전엔 부정문을 무시해서
우연히 맞았던 것을 이번에 정확하게 못 맞다고 판정한 것이라, 버그가 아니라
숨어있던 캡션 결함을 하나 더 찾아낸 셈이다.

**남은 한계**: 명시적 부정어("not", "no")만 잡고, 암묵적 반대 표현("정체
없이 원활하다"의 "원활"처럼 부정어 없이 반대 의미를 나타내는 경우)이나
부분 부정("정체는 아니지만 차가 많다")은 여전히 못 잡는다. 완벽한 해결이
아니라 가장 뚜렷한 사례만 걸러내는 저비용 완화책이다.

**빈도 측정(추가 조사)**: NLI 같은 더 무거운 방법을 도입할 가치가 있는지
판단하려고 빈도부터 쟀다. 전체 캡션 5,254문장 중 명시적 부정어(이미
완화책이 커버) 포함 문장은 281개(5.35%), "free of"/"clear of"/"moderate
traffic" 등 부정어 없이 "없음"을 뜻하는 암묵적 부정 후보는 75개(1.43%) -
대부분 "교통 혼잡 없음"류 표현에 몰려 있었다. 이 개념(교통 혼잡)은
현재 검증된 9개 프로덕션 검색어에 포함되지 않은 자유형 쿼리에서만
발견된 것이라, **검증된 검색어들은 이 문제에 노출돼 있지 않다.** 좁고
낮은 빈도(전체의 1.4% 수준)라 NLI 도입 근거로는 부족하다고 판단해
보류했다 - 측정 스크립트: `src/measure_negation_frequency.py`.

## 11. 캡션-영상 일치도(match_score)는 동작 서술에서 신뢰도가 낮다 (②)

**증거**: 처음 60클립(참/거짓 120점)으로 calibration/holdout 검증했고, 표본이
작아 신뢰구간이 넓다는 우려로 이후 200클립(참/거짓 600점)으로 재검증했다:

| 필드 | 60클립 accuracy/precision | 200클립 accuracy/precision |
|---|---|---|
| motion | 63.3% (거의 반반) / 60.5% | **68.5%** / 66.7% |
| weather | 71.7% / 63.8% | 71.5% / 65.5% |
| time_of_day | 96.7% / 93.8% | 97.0% / 97.0% |

*(코드리뷰로 n_frames=3/5 불일치를 잡은 뒤 재계산, 이후 200클립 재검증까지 반영한
최종 수치. "정지" 옵티컬 플로우 때(83.3%→85.0%, 거의 그대로)와 달리 이번엔 임계값과
정확도 둘 다 눈에 띄게 움직였다 - motion accuracy가 5.2%p 오르고, weather 임계값은
거의 4배(0.0102→0.0375) 이동했다. 60클립 기준값이 예상보다 더 불안정했다는 뜻 -
아래 15번 항목 참고.)*

한계 4(캡션 언어 의존성)와 별개로, **②(BLIP-ITM 기반 캡션-영상 검증)도 ①(CLIP)과
동일하게 동작/상태 구분에 약하다** — 두 모델 모두에게 공통된 근본 약점으로 보인다.
`is_video_verified`가 motion 관련 문장에서 False를 내든 True를 내든, 동전 던지기보다
살짝 나은 수준의 신뢰도로 받아들여야 한다.

**단, motion="stop"(정지) 주장만은 예외다** — 16번 항목에서 별도 신호(옵티컬 플로우)로
대체해 holdout accuracy 85.0%(n=200)까지 개선했다. 이 표의 68.5%는 "정지"를 제외한
나머지 motion 값(좌회전/우회전/직진/커브 차선 - 17번 항목에서 방향 확장을 시도했으나
채택 안 함)에 여전히 적용된다.

## 12. 필드 자동 분류는 키워드 매칭이라 동의어를 놓칠 수 있다 (②)

**증거**: `classify_sentence_field()`는 단순 키워드 매칭이다. "The vehicle's motion is
steady and consistent as it proceeds along the road." 같은 문장은 명백히 동작을
서술하지만 등록된 키워드("stopped", "turning left" 등)와 안 겹쳐서 `None`으로 분류되고,
통합 임계값(더 관대함)으로 fallback된다 — 실제 스팟체크에서 발견한 사례.

## 13. 악천후·시야 가림 상황에서는 정확한 캡션도 낮은 match_score를 받을 수 있다 (②)

**증거**: "다차선고속도로" 검색 결과 중, info.txt 기준 캡션이 정확한(실제
multi-lane highway) 클립이 `is_video_verified=False`로 나온 사례를 프레임까지 확인했다.
원인은 우천+근접 정체 차량+렌즈 위 빗방울로 화면이 심하게 가려져서, "다차선"이라는
시각적 증거 자체가 프레임에 안 보였기 때문이다. 즉 **낮은 match_score가 항상 "캡션이
틀렸다"를 의미하지 않는다** — 야간/우천/블랙박스 화질 같은 열악한 촬영 조건에서는
false negative(정확한 캡션을 틀렸다고 오판)가 나올 수 있다.

## 14. 필드 단위 세부 분석(detailed_claims)의 커버리지는 33%뿐이고, 넓히려는 시도는 실패/보류됐다 (②)

**증거**: 실제 캡션 문장(1263개) 중 motion/weather/time_of_day 중 하나라도 걸리는
비율은 33.0%뿐이다. 나머지 2개 필드(road_context/road_type)도 같은 방식으로
추가해봤지만: road_type은 검증 자체가 실패했고(참>거짓 45%, p=0.85, 순수 노이즈 —
①검색 단계의 "다차선고속도로 라벨 경계 모호성"이 재현됨), road_context는 수치상
강했지만(holdout 91.7%) 애초에 캡션 생성 시 정답이 그대로 주입되는 필드라 실제로
틀릴 일이 거의 없어서("당연히 일치"만 반복) 진단 목적에 별 도움이 안 됐다. 둘 다
추가하지 않기로 결정 — 지금 커버리지 33%가 정직한 최대치다.

**주행영상 적용 시 위험도**: `detailed_claims`로 나온 "세부 주장 없음"은 "이 부분은
확인 못 했다"는 뜻이지 "문제 없다"는 뜻이 아니다. 특히 객체(차량 종류/색상)나
road_context/road_type을 언급하는 문장의 사실 여부는 이 기능으로 전혀 커버되지 않는다.

## 15. [발견 및 수정됨] match_score 계산 조건이 컷오프 계산 조건과 달랐다 (②)

**증거**: `search_and_verify.py`(코드 리뷰, effort=high)로 발견 — match_score/컷오프를
계산한 보정 스크립트들(`validate_match_score.py`,
`calibrate_match_score_threshold.py`, `calibrate_road_fields_threshold.py`)은
전부 `n_frames=3`을 썼는데, 실제 프로덕션(`search_and_verify()`)의 기본값은
`n_frames=5`였다. match_score는 "여러 프레임 중 최댓값"이라 프레임을 더 뽑을수록
점수가 체계적으로 올라간다 — 15개 샘플로 직접 검증하니 3→5프레임으로 바꾸는 것만으로
2개(13%)의 판정이 "불일치→일치"로 뒤집혔고(반대 방향은 0건).

**조치**: `calibrate_match_score_threshold.py`를 n_frames=5로 재실행해서
`FIELD_MATCH_SCORE_THRESHOLDS`를 갱신했다(motion 0.1942→0.2128, weather
0.0099→0.0102, time_of_day 0.0649→0.0842, 통합 0.0379→0.0377). holdout
accuracy(신뢰도 등급의 근거)는 거의 그대로였다(motion/weather 동일,
time_of_day는 95.0%→96.7%로 오히려 개선) — **등급 자체는 안 바뀌었지만, 임계값
숫자가 실제 사용 조건과 안 맞았던 걸 통계적 재검증이 아니라 코드 리뷰로 잡아낸
사례**다. 통계적 검증(calibration/holdout)만으로는 이런 종류의 불일치를 못
잡는다는 교훈 — 코드 자체의 일관성 점검이 별도로 필요하다.

## 16. [해결됨] "정지" 판정은 옵티컬 플로우로 구조적 한계를 극복했다 (②)

**증거**: 30개 전수 감사(사용자와 함께 프레임을 직접 열어보며 진행)에서 "정지" 관련
캡션 1건이 오판됐다(#11). 원인을 추적한 결과, match_score(BLIP-ITM)가 프레임을
여러 장 봐도 서로 비교하지 않고 각자 독립적으로 채점하기 때문에, **"배경이 프레임
사이에 안 움직였다"(=자차가 정지해 있다)는 정지의 핵심 시각적 증거를 구조적으로 볼
방법이 없다**는 걸 확인했다 — 프레임 수를 늘려도, 재보정을 해도 못 고치는 종류의
문제였다(11번 항목과 같은 근본 원인의 특수 사례).

단순 픽셀 차이 비교는 1차 시도에서 실패했다(조명/노이즈에 취약). 정식 옵티컬 플로우
(`cv2.calcOpticalFlowFarneback`)로 배경(프레임 상단 35%, 다른 차량이 지나갈 도로
중앙부는 제외) 영역의 프레임 간 평균 흐름 크기를 계산해 별도 신호로 추가했다. 처음엔
60클립 calibration/holdout(30/30)으로 검증(holdout accuracy 83.3%)했는데, 표본이
작아 신뢰구간이 넓다는 우려(대략 66~93%)로 200클립(정지 100 + 그 외 100)
calibration/holdout(100/100)으로 재검증했다: **holdout accuracy 85.0%, precision
86.0%, recall 84.3%** — 결론은 그대로 유지되고 수치도 소폭 개선(정밀도가 오르고
신뢰구간도 좁아짐)됐다. 기존 BLIP-ITM 방식의 motion 전체 정확도(63.3%, 11번
참고)보다 뚜렷하게 개선됐다.

**조치**: `search_and_verify.py`에서 motion="stop" 주장에 한해서만 옵티컬 플로우
신호(`OPTICAL_FLOW_STOP_THRESHOLD=3.1960`)로 판정을 대체했다(신뢰도 등급은 3단계
체계를 유지하기 위해 보수적으로 "보통"으로 매겼지만, 실제 근거 수치는 85.0%로 통상
"보통" 등급보다 우수하다). 이 수정이 실제로 영향을 미치는 범위(motion="stop" 관련
결과)만 좁혀 재검증해서 부작용 없음을 확인했다(200클립 재보정 이후에도 재확인).
"정지" 외 motion 값(좌회전/우회전/직진/커브 차선)은 여전히 기존 BLIP-ITM 방식을
쓴다 — 17번 항목 참고.

**의의**: 재보정(calibration 데이터를 더 모으거나 임계값만 조정)으로는 원천적으로
해결 불가능한 구조적 한계였다는 게 핵심이다 — 프레임 간 비교라는 다른 검증 메커니즘을
도입해야만 풀리는 문제였고, 실제로 그렇게 풀었다.

## 17. [실험함, 미채택] 좌회전/우회전 방향 옵티컬 플로우는 쓸 만한 신호가 아니었다 (②)

**증거**: 16번의 "정지" 성공 이후, 같은 옵티컬 플로우 아이디어를 좌회전/우회전에도
확장할 수 있는지 확인했다 — 배경 영역 평균 흐름의 "수평 성분" 부호가 회전 방향을
반영할 것이라는 가설. 회사 전체 데이터셋에 있는 좌회전(8개)·우회전(15개) **전량**으로
calibration/holdout 검증했다(moving straight를 대조군으로 사용):

| 방향 | holdout accuracy | precision | recall |
|---|---|---|---|
| 우회전 | 70.6% | 100.0% | 37.5% |
| 좌회전 | 64.3% | 60.0% | 50.0% |

**원인 추정**: "정지"는 배경 흐름이 "거의 0이냐 아니냐"라는 단순한 이진 신호라
깨끗하게 갈리지만, 좌/우회전은 회전 각도·타이밍(5개 프레임 중 어느 구간에 회전이
몰려 있는지)·도로 폭에 따라 수평 흐름의 크기와 부호가 크게 흔들려 노이즈에 훨씬
취약한 것으로 보인다.

**결정: 프로덕션에 통합하지 않음.** 우회전은 판정을 내리면 틀린 적은 없지만(precision
100%) 대부분의 경우 판정 자체를 못 내리고(recall 37.5%), 게다가 전체 데이터에
우회전이 15개뿐이라 실사용 영향이 미미하다. 좌회전은 accuracy 64.3%가 반반 도박
수준이라 신호로 보기 어렵다. 좌/우회전 데이터는 전체 데이터셋에 있는 걸 이미 전량
사용했으므로(8개/15개 전부), 표본을 늘려 재시도할 여지도 없다 — 이 데이터 규모에서는
사실상 최대치다.

**주행영상 적용 시 위험도**: motion="turning left"/"turning right" 주장은 여전히
기존 BLIP-ITM 방식(motion 전체 holdout accuracy 63.3%)에 의존한다 — 반반에 가까운
신뢰도로 받아들여야 한다(11번 참고).

## 18. [조사함, 결론 안 남] 길고 복잡한 문장이 낮은 match_score를 받는 경향 - 문장 길이만으로는 설명 안 됨 (②)

**증거**: 30개 전수 감사에서 나온 오류 3건 중 나머지 2건(#26, #27, 자유형 쿼리
"The vehicle is turning at an intersection."의 검색 결과)을 조사했다. 둘 다
실제로는 교차로 장면이 정확히 담긴 클립인데 match_score가 극단적으로 낮았다:

| # | matched_sentence | match_score |
|---|---|---|
| 26 | "The vehicle is navigating through a busy intersection, with multiple lanes and traffic signals visible ahead." | 0.0275 |
| 27 | "The vehicle is moving through a busy intersection where other cars are also present, including a white SUV and a black sedan, both moving in the same direction." | 0.0059 |

두 문장을 더 짧게 바꿔가며 다시 채점하니 점수가 뚜렷이 올라갔다(#26:
0.0275→0.1453→0.6162, #27: 0.0059→0.1071→0.5791) - "BLIP-ITM이 길고 복잡한
문장을 체계적으로 낮게 채점한다"는 가설을 강하게 뒷받침하는 듯 보였다.

**반증**: 같은 방식으로 이전에 정확히 맞았던 사례(#8, "비" 검색,
"The sky is overcast, suggesting a rainy day, and the overall scene conveys
a typical urban commute during the day." - 20단어, match_score 0.2370)를
짧게 바꿔 재검증하니 오히려 **반대로** 점수가 떨어졌다(원문 0.2370 >
짧은 버전 0.0563/0.0949). 문장 길이 가설과 정반대 결과다.

**결론**: 문장 길이가 유일한 원인은 아니다. "night"처럼 유난히 강한 시각적
단서가 되는 단어의 유무 등 문장별 개별 어휘가 #26/#27 테스트 결과를
오염시켰을 가능성이 높다고 추정하지만, 확정하지 못했다. 일반화 가능한
패턴이 아니라고 판단해 추가 조치 없이 보류했다 - 정직하게 "부분적으로만
맞았고 결론 안 남"으로 기록한다.

## 19. [재검증됨] match_score 필드별 임계값을 200클립으로 재보정 - motion 정확도가 5.2%p 개선됨 (②)

**증거**: 11번/15번 항목의 근거였던 match_score 필드별 임계값은 60클립(참/거짓
120점, holdout 30클립)에서 나온 것이었다. "정지" 옵티컬 플로우를 200클립으로
재검증해서 신뢰구간을 좁힌 것과 같은 이유로, 이 기본 임계값들도 표본이 작아
신뢰구간이 넓을 수 있다는 우려가 있었다. `calibrate_match_score_threshold.py`의
`N_CLIPS`를 60→200으로 늘려 재검증했다(참/거짓 600점, holdout 100클립, 소요
약 62분 - GPU로 BLIP-ITM을 프레임마다 돌려야 해서 옵티컬 플로우 재검증보다
훨씬 오래 걸렸다):

| 필드 | 60클립 임계값/accuracy | 200클립 임계값/accuracy |
|---|---|---|
| 통합 | 0.0377 / 72.8% | 0.0729 / 73.7% |
| motion | 0.2128 / 63.3% | **0.2553 / 68.5%** |
| weather | 0.0102 / 71.7% | **0.0375** / 71.5% |
| time_of_day | 0.0842 / 96.7% | 0.0957 / 97.0% |

"정지" 옵티컬 플로우 재검증 때(83.3%→85.0%, 임계값도 거의 그대로)와 달리, 이번엔
임계값과 정확도 둘 다 눈에 띄게 움직였다 - weather 임계값은 거의 4배(0.0102→0.0375),
motion accuracy는 5.2%p(63.3%→68.5%) 올랐다. **60클립 기준값이 예상보다 더
불안정했다는 뜻**으로, 이번 재검증이 "정지" 때보다 오히려 더 값어치가 있었다.

**조치**: `search_and_verify.py`의 `MATCH_SCORE_THRESHOLD`, `FIELD_MATCH_SCORE_THRESHOLDS`,
`FIELD_CONFIDENCE_TIER`의 근거 주석을 전부 200클립 수치로 갱신했다. motion이
68.5%로 올라 weather/통합(71.5~73.7%, "보통")과의 격차가 좁아졌지만, 여전히
뚜렷한 차이가 있어 "낮음" 등급은 유지했다. 이 변경이 실제로 영향을 미치는 범위
(30개 전수 감사 결과, motion="stop" 제외)만 좁혀 재검증한 결과 **판정이 바뀐
사례는 0건**이었다 - 안전하게 반영됨을 확인.

## 20. [제품화] CLI 파일럿과 인덱스 캐싱을 추가했다 (②, 실험이 아니라 엔지니어링)

**계기**: 지금까지 `search_and_verify()`는 파이썬 스크립트 안에서 직접 호출해야만
결과를 볼 수 있었다. 실사용(파일럿)으로 넘어가면서 두 가지 갭을 확인했다:
1) 이미 `run_pilot.py`라는 스크립트가 있었지만, 이름과 달리 **1단계
(`full_inspector.py`, MSVD 공개데이터로 보정돼 회사 데이터엔 안 맞는 옛
파이프라인)용**이었다 - 최종 통합 파이프라인(`search_and_verify()`)용 CLI가
따로 없었다는 뜻. 2) 검색 인덱스(캡션 임베딩)가 실행할 때마다 클립 전체를
처음부터 다시 계산해서, 매번 몇 분씩 걸렸다(740클립 기준).

**조치**: `src/run_search_pilot.py`(신규)로 `search_and_verify()`용 CLI를
만들었다 - 검색어 하나로 ①②를 전부 돌리고 결과를 터미널/CSV로 보여준다.
`search_module.py`에 `build_cached_index()`를 추가해 클립 목록 +
`caption.txt` 수정시각/크기로 만든 지문(fingerprint)을 캐시 키로 쓴다 -
같은 데이터셋이면 재계산 없이 캐시에서 불러온다(실측: 100클립 기준
5.1초→0.2초). 새 데이터가 들어오면(클립/캡션 내용이 바뀌면) 지문이
자동으로 달라져 그 시점엔 한 번은 다시 빌드하고, 그 이후부터 다시 캐싱
효과를 본다 - 캐싱으로도 피할 수 없는 1회성 비용이라는 걸 인지하고
설계함. 캐시가 무한정 쌓이지 않도록 `_prune_index_cache()`로 최근 사용
순(LRU) 최대 5개(조정 가능)만 남기고 자동 정리한다 - 서로 다른 데이터셋
7개로 오래된 2개가 삭제되는 것과, 재조회한 캐시가 더 최신에 만들어진
다른 캐시보다 먼저 살아남는 것까지 직접 확인했다.

## 21. [제품화] 새 데이터셋 자동 인식 - 매니페스트 스캔 도구를 추가했다 (①, 실험이 아니라 엔지니어링)

**계기**: `outputs/search_full_dataset.json`(클립 목록 + info.txt 값)이
정적 스냅샷이었다 - 이걸 만든 스캔 스크립트가 코드베이스에 없어서, 새
클립이 생겨도 이 파일이 저절로 갱신되지 않았다(20번 항목의 캐싱은 "이미
목록에 있는 클립의 내용이 바뀌었는지"만 감지하지, "목록에 없던 새 클립이
생겼는지"는 감지 못 함).

**조치**: `src/scan_dataset.py`(신규)가 루트 디렉토리를 재귀 스캔해서
`caption.txt`+`info.txt`가 둘 다 있는 폴더를 클립으로 인식하고, info.txt
(motion/road_context/road_type/time_of_day/surface/weather 6줄 고정
순서)를 파싱해 매니페스트 JSON을 새로 만든다. 매번 전체를 재스캔하는
방식이라(부분 병합 없음) 삭제된 클립도 자동으로 목록에서 빠진다. 실제
회사 mount(740클립)로 스캔해서 기존 매니페스트와 **경로 집합·필드값
전부 완전히 일치**하는 것(0건 불일치)과, info.txt 누락/형식 오류가 있는
가짜 클립을 정확히 건너뛰며 사유를 보고하는 것까지 확인한 뒤, 실제
`outputs/search_full_dataset.json`도 이 스크립트로 재생성해서 교체했다
(내용 동일, "추가 0개/제거 0개"로 확인).

**설계 판단**: 새 데이터 유입 빈도가 아직 낮은 로컬 파이프라인 단계라,
이벤트 기반(S3 트리거 등) 대신 스캔 기반을 택했다 - "감지 방법"과
"이후 처리(캐싱/검색)"를 매니페스트 JSON으로 깔끔히 분리해뒀기 때문에,
나중에 실시간성이 필요해지면 감지 방법만(스캔→이벤트) 교체하면 되고
뒷단은 안 건드려도 된다.

**후속 수정**: `run_search_pilot.py`가 `scan_dataset.py`의 존재를 몰라서,
매니페스트 JSON이 없을 때 그냥 "못 찾았다"고만 하고 끝났다 - 실제로
써보다가 발견한 사용성 문제. 에러 메시지에 `scan_dataset.py` 실행 예시를
추가하고, 스크립트 상단 사용법 주석에도 두 도구의 관계를 명시했다(둘을
하나로 합치지는 않음 - 스캔은 데이터 관리, 검색은 질의라는 별개 관심사라
분리 유지가 맞다고 판단).

## 22. [실험 후 구현] 관련성 피드백(Rocchio 쿼리 재조정) - 자동 안전장치는 못 찾았지만, "나란히 보여주기"로 안전하게 구현했다 (①)

**계기**: NVIDIA Cosmos Dataset Search의 "linear probe query learner"(사용자가
결과에 관련있음/없음을 표시하면 쿼리를 그 방향으로 재조정)를 비교
분석하다가, 이 프로젝트 임베딩 공간에도 같은 아이디어(정보검색의 고전
기법인 Rocchio 알고리즘과 동일)를 적용할 수 있는지 확인했다.

**1차 검증(예비, 쿼리 4개)**: info.txt로 자동 라벨링해서(사람이 직접
라벨링할 필요 없이 "이 기법이 통하는지"부터 저비용 확인) "야간"/"정지"
쿼리에서 +80~90%p라는 강한 개선을 확인했다.

**2차 검증(정식, 9개 검증 앵커 쿼리 전부 + alpha 4개 값)**: 범위를
넓히자 훨씬 복잡한 그림이 나왔다 - "정지"/"야간"/"눈"은 확실히
개선됐지만(+30~90%p), "터널"은 alpha가 커질수록 오히려 나빠졌고(+10%p→
-20%p), "다리"는 아예 역효과(-10%p), "우회전"은 무변화였다. **처음
제안했던 안전장치 가설("본 결과의 긍정/부정 비율이 쏠리면 위험하다")을
직접 검증하니 반증 사례가 나와 기각됐다** - "눈"과 "다리"가 똑같이
극단적으로 쏠려 있었는데(1:9) 하나는 크게 개선되고 하나는 나빠졌다.
표본 7개로는 성공/실패를 예측하는 신뢰할 만한 규칙을 못 찾았다.

**최종 설계**: "언제 믿을지 예측"하는 대신, **원본 순위와 피드백 반영
순위를 항상 나란히 보여주고 사람이 직접 판단**하게 만들었다 - 예측
규칙이 틀려도 사람이 결과를 보고 바로 판단할 수 있으니 안전하다. 실제로
성공 사례("야간": 1/10→9/10 정답)와 실패 사례("다리": 1/10→0/10)를
나란히 보여주는 데모로, 두 경우 다 사람이 체크마크만 보면 어느 쪽이
나은지 명확히 판단 가능함을 확인한 뒤 구현했다.

**구현**: `search_module.py`에 `embed_query()`(쿼리를 벡터로만 변환),
`refine_embedding_with_feedback()`(Rocchio 재조정), `search_by_embedding()`
(벡터로 직접 검색) 추가 - 기존 `search()`는 내부 로직을 `_rank_by_embedding()`
으로 분리해 재사용하도록 리팩터링했다(리팩터링 후 3개 검증 쿼리로 기존
출력과 정확히 일치하는지 회귀 테스트 완료 - 회귀 없음). `run_search_pilot.py
--feedback`으로 대화형 모드를 추가해서, 라벨(y/n) 입력→재조정→나란히
비교 출력까지 실제 실행해서 확인했다(라벨 부족 시 안전하게 건너뛰는
경로, 입력이 중간에 끊기는 경우까지 포함).

**범위**: ①검색에만 적용된다. ②(캡션-영상 검증)와는 무관하다.

---
---

# 타임라인 일관성 체크 (temporal_consistency_check.py)

캡션 문장 순서가 영상의 실제 시간 순서와 얼마나 일치하는지 재는 별도 축. ①②
파이프라인에는 포함되지 않고 (검색어와 무관한, 캡션-영상 쌍 자체의 정적 속성이라)
독립된 오프라인 지표로 다뤄야 한다.

## 23. 효과가 작고, 측정 도구 자체의 오차도 섞여 있어 아직 QA 체크에 못 쓴다

**증거**: 회사 실데이터 180클립에서 이벤트성 문장(회전/진입/정지 등)만 놓고
순서 일치율을 재면 60%(무관계 50% 대비 통계적으로 유의, p=0.0222)가 나오지만,
이건 "신뢰할 수 있다"고 하기엔 낮은 수치다(동전 던지기보다 약간 나은 정도).
게다가 이 60%조차 순수하지 않다 - 이벤트 문장 11건의 진짜 정답 프레임을 직접
프레임을 보고 라벨링해서 BLIP-ITM의 선택과 비교하니, 정확히 일치는 27.3%뿐이고
평균 오차가 1.27프레임(영상 길이의 약 11%)이었다. 즉 60%의 "불일치" 중 일부는
캡셔닝이 아니라 **측정 도구 자체가 잘못된 프레임을 정답으로 골라서** 생긴 것이다
(실제 사례: "지하차도에 접근한다"는 문장에 대해 도구는 이미 지하차도를 빠져나온
장면을 골랐다 - 실제 정답은 훨씬 이전 프레임이었음).

**주행영상 적용 시 위험도: 현재는 사용 금지.** 캡션 순서를 신뢰의 근거로 삼거나
개별 문장 단위 PASS/FAIL 판정에 쓰기엔 근거가 부족하다. 도구 오차와 캡셔닝 오차를
분리하려면 그라운드트루스를 더 많이(30건 이상) 라벨링해야 한다.

## 원본 근거 데이터 (타임라인)

- 180클립 이벤트 필터링 재검증: `outputs/temporal_event_filtered_summary.csv`,
  `outputs/temporal_per_sentence.csv` (temporal_consistency_event_filtered.py)
- 도구 정확도 직접 검증(11건): `outputs/temporal_tool_accuracy.csv`
  (make_temporal_grids.py + 수동 라벨링)

## 원본 근거 데이터 (검색+검증 통합)

- 검색 대규모 실험: `outputs/search_largescale_*.csv`, `outputs/search_largescale_caption_*.csv`
- 컷오프 보정(100→740클립): `outputs/search_thresholds_full.json` (calibrate_search_thresholds_full.py)
- match_score 참/거짓 검증: `outputs/match_score_validation.csv` (validate_match_score.py)
- match_score 컷오프 보정(최종, 200클립): `outputs/match_score_threshold_result.json`,
  `outputs/match_score_calibration.csv` (calibrate_match_score_threshold.py) - 1차
  60클립 결과는 `outputs/match_score_threshold_result_n60_archive.json` /
  `outputs/match_score_calibration_n60_archive.csv`에 보관
- 30개 전수 감사(사람이 프레임 직접 확인): `/tmp/audit_results.pkl` (세션 한정, 재현하려면 감사 절차를 다시 실행해야 함)
- "정지" 옵티컬 플로우 실험/보정(최종, 200클립): `src/experiment_optical_flow_motion.py`,
  `src/calibrate_optical_flow_threshold.py`, `outputs/optical_flow_calibration.csv`,
  `outputs/optical_flow_threshold_result.json` (1차 60클립 결과는
  `outputs/optical_flow_calibration_n30_archive.csv` /
  `outputs/optical_flow_threshold_result_n30_archive.json`에 보관)
- 좌/우회전 방향 옵티컬 플로우 보정(미채택): `src/calibrate_turn_direction_flow.py`,
  `outputs/turn_direction_calibration.csv`, `outputs/turn_direction_threshold_result.json`
- 최종 모듈: `src/search_module.py`(①, `build_cached_index()` 인덱스 캐싱 +
  `embed_query()`/`refine_embedding_with_feedback()`/`search_by_embedding()`
  관련성 피드백 포함), `src/search_and_verify.py`(②+통합)
- 파일럿 CLI: `src/run_search_pilot.py` (옛 `run_pilot.py`는 1단계 `full_inspector.py`용 - 별개, `--feedback`으로 관련성 피드백 대화형 모드)
- 데이터셋 매니페스트 스캔: `src/scan_dataset.py` (새 데이터셋 자동 인식)
- 관련성 피드백 실험/검증: `src/experiment_relevance_feedback.py`(예비, 쿼리 4개),
  `src/calibrate_relevance_feedback.py`(정식, 9개 앵커 쿼리 + alpha 4개)
- 종합 리포트(2단계 이후): `results/final_report.md`
